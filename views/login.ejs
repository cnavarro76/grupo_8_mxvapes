<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <script src="https://kit.fontawesome.com/7a57e2dd08.js" crossorigin="anonymous"></script>

        <title>Login MXVapes</title>
 
 <link rel="stylesheet" href="/css/styleLogin.css">
      <%- include('./partials/script-header') %>
    </head>
    <body class="fondo"> 



  <%- include('./partials/header') %>
<%- include('header') -%>

            <div class="panel-body">
                <form method="POST" action="/users/login" class="formulario">
 <h1 class="formularios-t">Iniciar sesión</h1>
            <div>
              <img src="../img/LogReg/user.png" class="img"/><br>
            </div>

            <div class="input-contenedor">
 
              <input type="text" name="email" placeholder="Correo Electronico" class="form-control <%= locals.errors && errors.email ? 'is-invalid' : null %>" value="<%= locals.oldData ? oldData.email : null %>"><i class="fas fa-envelope"></i>
                  <% if (locals.errors && errors.email) { %>
                      <div class="text-danger">
                          <%= errors.email.msg %>
                      </div>
                  <% } %>
              </div>
              <div class="input-contenedor">

                <div class="input-contenedor-password">
                  
                  <input type="password" name="password" placeholder="Contraseña" class="form-control <%= locals.errors && errors.password ? 'is-invalid' : null %>"> <i class="fas fa-key"></i>
                  <% if (locals.errors && errors.password) { %>
                    <div class="text-danger">
                        <%= errors.password.msg %>
                    </div>
                <% } %>
                
              </div>
              <label class="form-check-label"><input type="checkbox" class="form-check-input" name="recuerdame">Recordar mis usuario</label>


                    <button class="boton" type="submit"><i class="fa fa-sign-in"></i>
                      Iniciar sesión</button>
                    <div>
                          
                      <p><br>¿Olvidaste tu <a class="link" href="#">contraseña</a>?</p> 
                    </div>
                </form>
                 <form class="formulario2">
          <p>¿No posees una cuenta? <a class="link" href="/users/register">Crea una</a></p>
          <p>o inicia sesión con</p>
          <p class="iconos">
            <script src="//ajax.googleapis.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
            <a href="#"><i class="fab fa-google"></i></a>
            <a href="#"><i class="fab fa-facebook"></i></a>
          </p>
          </form>
            </div>
        </div>
    </div>

<br>


  <%- include('./partials/footer') %>


<%- include('./partials/menu') %>

</body>
</html>
